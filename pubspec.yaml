name: groupchat
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  shelf: ^1.4.2
  dio: ^5.9.0
  hive_ce: ^2.12.0
  hive_ce_flutter: ^2.3.2
  dart_duckdb: ^1.2.2
  path_provider: ^2.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  test: ^1.26.2

flutter:
  uses-material-design: true
